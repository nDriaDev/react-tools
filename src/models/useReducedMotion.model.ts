/**
 * Whether the `prefers-reduced-motion` media query is active, as returned by [useReducedMotion](https://react-tools.ndria.dev/hooks/api-dom/useReducedMotion).
 * `true` when the user has requested reduced motion; `false` otherwise.
 */
export type UseReducedMotionResult = boolean;